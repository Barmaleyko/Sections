<%= form_for(@heading) do |f| %>
    <% if @heading.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@heading.errors.count, "error") %> prohibited this heading from being saved:</h2>

          <ul>
            <% @heading.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :name %><br>
      <%= f.text_field :H_Name %>
    </div>
    <div class="field">
      <%= f.label :phone %><br>
      <%= f.text_field :H_Phone %>
    </div>
    <div class="field">
      <%= f.label :address %><br>
      <%= f.text_field :H_Address %>
    </div>
    <div class="field">
    <%= select_tag 'heading[section_id]', options_for_select(@sectionn.collect{ |s| [s.Sec_Name, s.id] } ) %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
